#! /bin/bash

rm ntpdrift.data
while true
do 
  clear
  ntpq -c "rv 0 offset" | cut -d "=" -f 2 >> ntpdrift.data
  gnuplot -e 'set terminal dumb; set ylabel "NTP Drift (ms)"; set xlabel "Number"; plot "< tail -20 ntpdrift.data" with lines;'
  sleep 60
done;
